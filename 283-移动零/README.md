# 283. 移动零

## 题目描述

[283-移动零](https://leetcode.cn/problems/move-zeroes/)

## 思路

首先两个指针都指向数组的第一个元素，右指针不断右移，直到遇到非零元素。左指针只有在右指针遇到非零元素时，与右指针所指元素交换，然后右移。这样我们可以发现，左指针左边的一定都是非零元素，左右指针之间的元素都为零。所以每次交换都没有改变数组的非零元素的顺序。

## 复杂度

- 时间复杂度：O(n)
- 空间复杂度：O(1)
