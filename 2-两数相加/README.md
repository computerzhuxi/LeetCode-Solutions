# 2. 两数相加

## 题目描述

[2- 两数相加](https://leetcode.cn/problems/add-two-numbers/)

## 思路

思路很简单，因为是从低位到高位。

我们同时遍历两个链表，逐位计算它们的和，并与当前位置的进位值相加。

具体而言，我们先令当前节点的值和为进位，将两链表当前节点不为空的数加上，让和 mod 10 来创建新的节点，放到链表表尾。进位为和除10。直到两链表指针都为空且进位为0

## 复杂度

- 时间复杂度：O(max(n,m))
- 空间复杂度：O(1)
